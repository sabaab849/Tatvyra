# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Working source art — 28MB of PNGs that nothing imports. The served copies
# are converted into public/images/, and OneDrive locking these is what kept
# crashing the dev server (see server.watch.ignored in vite.config.js).
product images/

# Local build output and scratch
.vite/
